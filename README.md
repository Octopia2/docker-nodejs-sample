# Installation des Projekts

## Repository klonen

Als Erstes habe ich das Repository mit Git auf meinen Computer geklont. Dafür habe ich den Befehl `git clone <REPOSITORY_URL>` verwendet. Danach habe ich mit `cd <PROJEKTVERZEICHNIS>` in den Projektordner gewechselt.

## Pakete installieren

Die nötigen Pakete habe ich dann auch noch nach geschaut und installiert. Ich musste aber zuerst node installieren und VS-studios code die Berechtigung geben, damit ich es hier installieren konnte.

## Docker Desktop

Docker Desktop haben wir bereits gestern installiert und konfiguriert. Deshalb musste ich Docker Desktop heute nicht erneut installieren oder einrichten.

## Docker Desktop testen

Ich habe Docker Desktop getestet, indem ich in meinem Terminal das `hello-world` Command an Docker Desktop geschickt habe. Dadurch konnte ich überprüfen, ob Docker Desktop richtig funktioniert und Befehle aus dem Terminal verarbeitet.
